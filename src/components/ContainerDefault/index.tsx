import React from 'react';

import { Container } from './styles';

const ContainerDefault: React.FC = ({ children }) => {
  return <Container>{children}</Container>;
};

export default ContainerDefault;
